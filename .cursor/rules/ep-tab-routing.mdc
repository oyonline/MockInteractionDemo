---
description: EPoseidon2.0 Tab Switch routing - use for all App.js and page component modifications
globs: src/App.js, src/pages/**/*.js
alwaysApply: false
---

# EPoseidon2.0 Tab Switch Routing

## Tab 数据结构
{ id, name, path, data? }

## 添加新页面步骤

1. 创建页面文件于 `src/pages/{module}/PageName.js`
2. App.js 顶部 import 页面组件
3. 在 `renderTabContent()` 添加 case

### 概览页面模板
```javascript
case '/{module}/overview':
    return (
        <ModuleLayout>
            <XxxOverviewPage />
        </ModuleLayout>
    );
```

### 列表页模板
```javascript
case '/{module}/list':
    return (
        <ModuleLayout>
            <XxxListPage onOpenDetail={openTab} />
        </ModuleLayout>
    );
```

### 详情页模板（无 ModuleLayout）
```javascript
case '/{module}/detail':
    return <XxxDetailPage data={tab.data} onClose={() => closeTab(tab.id)} />;
```

## 页面类型规范

| 类型 | Layout | 路径模式 | 示例 |
|------|--------|----------|------|
| 概览页 | ModuleLayout | `/{module}/overview` | `/sales/us/overview` |
| 列表页 | ModuleLayout | `/{module}/list` | `/logistics/vendors` |
| 详情页 | 无（全宽） | `/{module}/detail` | `/logistics/vendors/detail` |

## 禁止事项
- ❌ 不使用 react-router-dom
- ❌ 不在 Tab 数据中存储 component
- ❌ 不修改 Layout/TabBar/Header 结构
